{
  "name": "ai-cases-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Static premium cases landing page",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node server.js --root dist",
    "start": "node server.js --root dist",
    "preview": "npm run build && node server.js --root dist"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  }
}
