# ——— Frontend: подставляется в dist при сборке (npm run build). Для GitHub Pages задайте в Actions → Variables. ———
LEADS_ENDPOINT=

# ——— Serverless (Cloudflare Worker): только через wrangler secret put, не коммитьте сюда ———
# BOT_TOKEN=
# ADMIN_CHAT_ID=
# ALLOWED_ORIGIN=
